services:
  app:
    build:
      context: .
      target: dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules        # keep container node_modules isolated
    env_file:
      - .env.local
    restart: unless-stopped
